caddy_version: 1.0.1
caddy_module: "github.com/caddyserver/caddy@v{{ caddy_version }}"
caddy_build: "{{ playbook_dir }}/tmp/caddy"
caddy_binary: "{{ caddy_build }}/caddy"
caddy_user: www-data
caddy_conf_dir: /etc/caddy
caddy_log_dir: /var/log/caddy
caddy_certs_dir: /etc/ssl/caddy
caddy_bin_dir: /usr/local/bin
caddy_home_dir: /home/caddy
